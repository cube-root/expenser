import axios from "axios";
const SHEET_URL = 'https://sheets.googleapis.com/v4'
const getSpreadSheet = async(accessToken:String,spreadSheetId:String)=>{
    try {
        // const response = await axios
        // .get(`${SHEET_URL}/`)
    } catch (error) {
        
    }
}