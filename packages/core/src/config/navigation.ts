import { HomeIcon } from '@heroicons/react/outline';

const navigation: Navigation = [
  {
    id: 'home',
    name: 'Home',
    href: '/home',
    icon: HomeIcon,
  },
];

export default navigation;
